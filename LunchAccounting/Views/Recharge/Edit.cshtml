@model LunchAccounting.Models.FoodCardRecharge

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>FoodCardRecharge</legend>

        <div>
            @Html.HiddenFor(model => model.FoodCardRecharge_id)
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.FoodCard_id, "Food Card")
            @Html.DropDownList("FoodCard_id", null, new { @class = "form-control " })
            @Html.ValidationMessageFor(model => model.FoodCard_id)
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Amount)
            @Html.TextBoxFor(model => model.Amount, null, new { @class = "form-control " })
            @Html.ValidationMessageFor(model => model.Amount)
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Operator, "Operator")
            @Html.DropDownList("Operator", null, new { @class = "form-control " })
            @Html.ValidationMessageFor(model => model.Operator)
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.OperateFor, "OperateFor")
            @Html.DropDownList("OperateFor", null, new { @class = "form-control " })
            @Html.ValidationMessageFor(model => model.OperateFor)
        </div>

        <p>
            <input type="submit" value="Save" class="btn btn-sm btn-primary" />
            @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-sm btn-primary" })
        </p>
    </fieldset>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
