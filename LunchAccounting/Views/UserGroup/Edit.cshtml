@model LunchAccounting.Models.UserGroup

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>UserGroup</legend>

        <div>
            @Html.HiddenFor(model => model.UserGroup_id)
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.GroupName)
            @Html.TextBoxFor(model => model.GroupName, null, new { @class = "form-control " })
            @Html.ValidationMessageFor(model => model.GroupName)
        </div>

        <p>
            <input type="submit" value="Save" class="btn btn-sm btn-primary" />
            @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-sm btn-primary" })
        </p>
    </fieldset>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
